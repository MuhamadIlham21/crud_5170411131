package com.app.id.crud_5170411131

class fakultas {
    var id_fakultas: Int = 0
    var kode_fakultas: String = ""
    var nama_fakultas: String = ""

    constructor()

    constructor(id_fakultas: Int, kode_fakultas: String, nama_fakultas: String) {
        this.id_fakultas = id_fakultas
        this.kode_fakultas = kode_fakultas
        this.nama_fakultas = nama_fakultas
    }
}